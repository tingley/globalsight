CREATE TABLE REMOVED_PREFIX_TAG
(
  ID BIGINT
     AUTO_INCREMENT
	 PRIMARY KEY,	 
  STRING 
     VARCHAR(4000)
	 NOT NULL,
  TU_ID BIGINT
     NOT NULL,
	 CONSTRAINT FK_REMOVED_PREFIX_TAG_TU_ID FOREIGN KEY(TU_ID) REFERENCES TRANSLATION_UNIT(ID) 
) AUTO_INCREMENT = 1000;

CREATE TABLE REMOVED_SUFFIX_TAG
(
  ID BIGINT
     AUTO_INCREMENT
     PRIMARY KEY,    
  STRING 
     VARCHAR(4000)
     NOT NULL,
  TU_ID BIGINT
     NOT NULL,
     CONSTRAINT FK_REMOVED_SUFFIX_TAG_TU_ID FOREIGN KEY(TU_ID) REFERENCES TRANSLATION_UNIT(ID) 
) AUTO_INCREMENT = 1000;