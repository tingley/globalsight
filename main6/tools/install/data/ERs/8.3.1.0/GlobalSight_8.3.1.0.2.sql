# GBS-2839: separate word count category for MT segments when MT engine is used

ALTER TABLE TARGET_PAGE ADD COLUMN MT_EXACT_MATCH_WORD_COUNT INT(10) DEFAULT 0; 

ALTER TABLE WORKFLOW ADD COLUMN MT_EXACT_MATCH_WORD_COUNT INT(10) DEFAULT 0; 
