#GBS-3930:GIT CONNECTOR: Changes made to files in repository while a translation job is in progress breaks the connector
ALTER TABLE CONNECTOR_GIT_CACHE_FILE ADD COLUMN SRC_FILE_PATH VARCHAR(300) NOT NULL;
ALTER TABLE CONNECTOR_GIT_CACHE_FILE ADD COLUMN DST_FILE_PATH VARCHAR(300) NOT NULL;