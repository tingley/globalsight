Once this application matures and is able to create projects on itself
(extract files, align files), the Aligner engine classes go here.

- generic interface for aligners
- factory to create aligner instances
- different implementations of aligners
- any support classes: term lists, language resources etc
